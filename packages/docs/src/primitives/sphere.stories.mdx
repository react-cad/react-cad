import { Source, Meta, Story } from "@storybook/addon-docs";
import { ArgsTable } from "@storybook/components";

<Meta title="Primitives/<sphere>" />

# sphere

The `sphere` element creates a sphere centered on the origin. It has a `radius`. An optional `angle` prop limits the shape to the portion of the sphere within that angle around the z-axis. A spherical segment can be produced with the optional `segmentAngle1` and `segmentAngle2` props.

<ArgsTable
  rows={{
    radius: {
      name: "radius",
      type: {
        required: true,
      },
      description: "The radius of the sphere.",
      table: {
        type: {
          summary: "number",
          detail: "a number greater than zero",
        },
      },
    },
    angle: {
      name: "angle",
      description: "The angle around the z-axis to limit the sphere to",
      table: {
        type: {
          summary: 'number',
          detail: 'a number between one and `2π`'
        },
        defaultValue: {
          summary: '2π'
        },
      },
    },
    segmentAngle1: {
      name: "segmentAngle1",
      description: "The angle above the xy plane where the top of the sphere is cut off to form a spherical segment",
      table: {
        type: {
          summary: 'number',
          detail: 'a number between `-π` and `π`'
        },
        defaultValue: {
          summary: 'π'
        },
      },
    },
    segmentAngle2: {
      name: "segmentAngle2",
      description: "The angle above the xy plane where the bottom of the sphere is cut off to form a spherical segment. Must be less than `segmentAngle1`",
      table: {
        type: {
          summary: 'number',
          detail: 'a number between `-π` and `segmentAngle1`'
        },
        defaultValue: {
          summary: '-π'
        },
      },
    },
  }}
/>

## Examples

export const Sphere = (props) => <sphere {...props} />;

### Sphere

<Source language="jsx" code={`
  <sphere radius={1} />
`} />

<Story name="sphere" args={{ radius: 1 }}>
  {Sphere.bind({})}
</Story>

### Sphere limited by an angle

<Source language="jsx" code={`
  <sphere radius={1} angle={3 * Math.PI / 2} />
`} />

<Story name="sphere limited by an angle" args={{ radius: 1, angle: 3 * Math.PI / 2 }}>
  {Sphere.bind({})}
</Story>

### Spherical segment

<Source language="jsx" code={`
  <sphere radius={1} segmentAngle1={1} segmentAngle2={-0.1} />
`} />

<Story name="spherical segment" args={{ radius: 1, segmentAngle1: 1, segmentAngle2: -0.1 }}>
  {Sphere.bind({})}
</Story>
