import { Source, Meta, Story } from "@storybook/addon-docs";
import { ArgsTable } from "@storybook/components";

<Meta title="Reference/Primitives/Torus" />

# torus

The `torus` element creates a torus centered on the origin. It has a radius from the origin to the center of the tube `radius1` and a radius of the tube `radius2`. An optional `angle` prop limits the shape to the portion of the torus within that angle around the z-axis.

<ArgsTable
  rows={{
    radius1: {
      name: "radius1",
      type: {
        required: true,
      },
      description: "The radius from the origin to the center of the tube. This must be greater than zero (for a degenerate torus use a sphere instead).",
      table: {
        type: {
          summary: "number",
          detail: "a number greater than zero",
        },
      },
    },
    radius2: {
      name: "radius2",
      type: {
        required: true,
      },
      description: "The radius of the tube.",
      table: {
        type: {
          summary: "number",
          detail: "a number greater than zero",
        },
      },
    },
    angle: {
      name: "angle",
      description: "The angle around the z-axis to limit the torus to",
      table: {
        type: {
          summary: 'number',
          detail: 'a number between one and `2π`'
        },
        defaultValue: {
          summary: '2π'
        },
      },
    },
  }}
/>

## Examples

export const Torus = (props) => <torus {...props} />;

### Torus

A torus with a radius of 3 and a tube radius of 1.

<Source language="jsx" code={`
  <torus radius1={3} radius2={1} />
`} />

<Story name="torus" args={{ radius1: 3, radius2: 1 }}>
  {Torus.bind({})}
</Story>

### Torus limited by an angle

A torus with a radius of 3 and a tube radius of 1, limited to 3 quarters of a turn.

<Source language="jsx" code={`
  <torus radius1={3} radius2={1} angle={3 * Math.PI / 2} />
`} />

<Story name="torus limited by an angle" args={{  radius1: 3, radius2: 1, angle: 3 * Math.PI / 2 }}>
  {Torus.bind({})}
</Story>

### Horn torus

A torus where the axis is a tangent to the cross section of the tube.

<Source language="jsx" code={`
  <torus radius1={2} radius2={2} />
`} />

<Story name="horn torus" args={{  radius1: 2, radius2: 2  }}>
  {Torus.bind({})}
</Story>

### Spindle torus

A torus where the axis passes through the cross section of the tube twice.

<Source language="jsx" code={`
  <torus radius1={2} radius2={3} />
`} />

<Story name="spindle torus" args={{  radius1: 2, radius2: 3 }}>
  {Torus.bind({})}
</Story>
