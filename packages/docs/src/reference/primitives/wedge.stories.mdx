import { Source, Meta, Story } from "@storybook/addon-docs";
import { ArgsTable } from "@storybook/components";

<Meta title="Reference/Primitives/Wedge" />

# wedge

The `wedge` element creates a slanted box, i.e. a box with angles. Width, length and height are controlled by the `x`, `y` and `z` props.

There are two ways to define the shape of the wedge:

- `ltx`  defines the width in the x-direction at `y`. The wedge has a constant height in the z-direction. [See example.](#wedge-defined-using-ltx)
- `xmin`,`zmin` and `xmax`,`zmax` define the corners of the wedge at `y`. [See example.](#wedge-defined-using-xmin-zmin-xmax-zmax)

The two methods are mutually exclusive, so the `ltx` prop must not be set if you are using the other method.

<ArgsTable
  rows={{
    x: {
      name: "x",
      type: {
        required: true,
      },
      description: "The width of the wedge in the x-axis",
      table: {
        type: {
          summary: "number",
          detail: "a number greater than zero",
        },
      },
    },
    y: {
      name: "y",
      type: {
        required: true,
      },
      description: "The length of the wedge in the y-axis",
      table: {
        type: {
          summary: "number",
          detail: "a number greater than zero",
        },
      },
    },
    z: {
      name: "z",
      type: {
        required: true,
      },
      description: "The height of the wedge in the z-axis",
      table: {
        type: {
          summary: "number",
          detail: "a number greater than zero",
        },
      },
    },
    ltx: {
      name: "ltx",
      description: "The width in the x-axis of the wedge at `y`. Can not be used alongside `xmin`, `zmin`, `xmax` and `zmax`",
      table: {
        type: {
          summary: 'number',
          detail: "a number greater than or equal to zero",
        },
      },
    },
    xmin: {
      name: "xmin",
      description: "The x-coordinate of the bottom-left corner of the wedge at `y`. Can not be used alongside `ltx`",
      table: {
        type: {
          summary: 'number'
        },
      },
    },
    zmin: {
      name: "zmin",
      description: "The z-coordinate of the bottom-left corner of the wedge at `y`. Can not be used alongside `ltx`",
      table: {
        type: {
          summary: 'number'
        },
      },
    },
    xmax: {
      name: "xmax",
      description: "The x-coordinate of the top-right corner of the wedge at `y`. Can not be used alongside `ltx`",
      table: {
        type: {
          summary: 'number'
        },
      },
    },
    zmax: {
      name: "zmax",
      description: "The z-coordinate of the top-right corner of the wedge at `y`. Can not be used alongside `ltx`",
      table: {
        type: {
          summary: 'number'
        },
      },
    },
  }}
/>

## Examples

export const Wedge = (props) => <wedge {...props} />;

### Wedge defined using ltx

A 5x5x3 wedge tapering to 1 unit wide at `y = 5`.

<Source language="jsx" code={`
  <wedge x={5} y={5} z={3} ltx={1} />
`} />

  <Story name="wedge" args={{ x: 5, y: 5, z: 3, ltx: 1 }}>
    {Wedge.bind({})}
  </Story>

### Wedge defined using xmin, zmin, xmax and zmax

A 5x5x3 wedge tapering to a square with corners at `(1,5,2)` and `(3,5,4)`.

<Source language="jsx" code={`
  <wedge x={5} y={5} z={5} xmin={1} zmin={2} xmax={3} zmax={4} />
`} />

  <Story name="wedge with xmin, zmin, xmax, zmax" args={{ x: 5, y: 5, z: 5, xmin: 1, zmin: 2, xmax: 3, zmax: 4 }}>
    {Wedge.bind({})}
  </Story>
