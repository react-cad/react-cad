import { Source, Meta, Story } from "@storybook/addon-docs";
import { ArgsTable } from "@storybook/components";
import reactIcon from './react-icon';

<Meta title="Reference/Sweeps/Helix" />

# helix

The `helix` element extrudes a 2-dimensional shape while rotating it around the z-axis. It has a `height` in the z-direction and a `pitch`, which is the height of one complete turn. The 2d shape is passed to the `profile` prop, which can either be a list of points describing a planar polygon, or a string containing an SVG image.

<ArgsTable
  rows={{
    height: {
      name: "height",
      description: "The height of the helix on the z-axis",
      type: {
        required: true,
      },
      table: {
        type: {
          summary: "number",
          detail: "a number greater than zero",
        },
      },
    },
    pitch: {
      name: "pitch",
      description: "The height of one complete turn of the helix.",
      type: {
        required: true,
      },
      table: {
        type: {
          summary: "number",
          detail: "a number greater than zero",
        },
      },
    },
    profile: {
      name: "profile",
      description: "The shape to be extruded.",
      type: {
        required: true,
      },
      table: {
        type: {
          summary: 'string | [number, number, number][]',
          detail: "an SVG image, or a list of [x, y, z] points describing a planar polygon",
        },
      },
    },
  }}
/>

## Examples

export const Helix = (props) => <helix {...props} />;

### Square helix

A helix of height 5 and pitch 5 with a square profile. Note that `pitch` refers to one full rotation; since the square has four points it is creates a multistart screw with a thread pitch smaller than 5!

<Source language="jsx" code={`
  <helix height={5} pitch={5} profile={[[1,1,0], [-1,1,0], [-1,-1,0], [1,-1,0]]} />
`} />

<Story name="helix" args={{ height: 5, pitch: 5, profile: [[1,1,0], [-1,1,0], [-1,-1,0], [1,-1,0]] }}>
  {Helix.bind({})}
</Story>

### Spring

A spring created using a vertical profile which does not intersect with the origin.

<Source language="jsx" code={`
  <helix height={5} pitch={2} profile={[[2,0,0], [2,0,1], [1,0,1], [1,0,0]]} />
`} />

<Story name="spring" args={{ height: 5, pitch: 2, profile: [[2,0,0], [2,0,1], [1,0,1], [1,0,0]] }}>
  {Helix.bind({})}
</Story>

### React icon helix

A partial turn of a helix with a complex SVG shape.

<Source language="jsx" code={`
  import reactIcon from './react-icon.svg';

<prism height={5} pitch={50} profile={reactIcon} />
`} />

<Story name="svg-helix" args={{ height: 5, pitch: 50, profile: reactIcon([0,0]) }}>
  {Helix.bind({})}
</Story>
