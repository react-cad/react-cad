import { ArgsTable, Meta, Story } from '@storybook/addon-docs';

export const Box = (props) => <box {...props} />;

<Meta title="Primitives/Box" component={Box} />

# box

The `box` element represents a cuboid with a width, length and depth. By default it is created at the origin and extends in the positive `x`, `y` and `z` directions, but it can optionally be centered on the origin.

<ArgsTable story="." />

## Default box

<Story id="primitives-box--box" />

## Centered box

<Story id="primitives-box--centered" />
