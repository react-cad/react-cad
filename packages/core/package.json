{
  "name": "@react-cad/core",
  "version": "1.0.0",
  "main": "lib/index.js",
  "author": "simlrh",
  "license": "LGPL",
  "scripts": {
    "build-dev-docker": "docker build -t react-cad-build .",
    "build-wasm": "docker run --rm -u=`id -u` -v $PWD:/src/react-cad react-cad-build",
    "prebuild": "cp src/*.d.ts lib",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/emscripten": "^1.39.4",
    "prettier": "^2.2.1"
  },
  "dependencies": {}
}
